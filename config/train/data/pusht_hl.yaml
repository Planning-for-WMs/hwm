dataset:
  num_steps: ${eval:'${wm.k} + 1'}   # K+1 sampled frames per sample
  frameskip: 5
  name: pusht_expert_train
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
  keys_to_cache:
    - action
    - proprio
    - state
